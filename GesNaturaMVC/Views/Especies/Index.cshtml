@model IEnumerable<GesPhloraClassLibrary.Models.Especie>

@{
    ViewBag.Title = "Index";
}

<h2>Espécies</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Genero
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeCientifico)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Calendario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abundancia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Genero.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeCientifico)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Calendario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abundancia)
        </td>
        @*<td>
            <img src="@Url.Content(item.Caminho)" width="300" height="200" />
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })

            @*@if (User.Identity.IsAuthenticated)
            {
                if (User.IsInRole("Admin,Supervisor"))
                {
            
                    @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                }
            }*@
        </td >
    </tr >
}

</table>
